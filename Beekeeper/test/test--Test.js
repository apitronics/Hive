var expect = require('chai').expect

describe("TelliTes", function() {
  describe("#go()", function() {
    this.timeout(15000)
    it("info about ...", function(done) {
      var err = null
      var newDoc = {}
      expect(err).to.equal(null)
      expect(newDoc).to.be.an('object')
      done()
    })
  })
})
